# UX decisions for the chat flows

Blocked by: 04
Type: grilling
Status: open

## Question

Conversation with the human, reacting to the UI prototype — settle the UX of each flow: login states (idle → connecting → failed, error display), friend-list interaction (select, when the list is empty), add-friend flow (search → send request → feedback), incoming friend-apply surfacing and approve/deny, and the chat pane (send message, how incoming 1015/1019-delivered text appears, empty-state guidance). Uses `/grilling` + `/domain-modeling`; resolves to concrete UI-state decisions the architecture ticket can implement.
