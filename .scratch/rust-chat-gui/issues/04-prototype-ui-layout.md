# Prototype the UI layout (login / main / add-friend)

Blocked by: 01
Type: prototype
Status: open

## Question

Produce a cheap, rough Slint prototype the human can react to — the three screens and their composition: the login screen (server host, user, password), the main window (friend list left, chat pane right), and the add-friend dialog (search result + request). How should the window be laid out, how should chat bubbles read, where do incoming friend-apply notifications surface? The prototype runs standalone on mock data — it does not need the network yet. Links the prototype artifact from the ticket on resolution.
