[package]
name = "iffchat-prototype"
version = "0.1.0"
edition = "2021"
build = "build.rs"
description = "THROWAWAY visual prototype for the Iffchat layout — no interaction logic."

[dependencies]
slint = "1.17.1"

[build-dependencies]
slint-build = "1.17.1"
